# macOS support utilities

## Installation

1. Copy `tray` binary to `/usr/local/bin/mail-notifier-tray`
2. Run `./macos/svc-install`

## Uninstallation

1. Run `./macos/svc-uninstall`
2. Remove `/usr/local/bin/mail-notifier-tray`
