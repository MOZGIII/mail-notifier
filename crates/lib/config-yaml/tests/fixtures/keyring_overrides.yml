servers:
  - name: "test server"
    host: "imap.example.com"
    tls:
      mode: implicit
    login:
      username: "user@example.com"
      password:
        keyring:
          service: "mail-notifier"
          account: "user@example.com"
    mailboxes:
      - name: "INBOX"
