servers:
  - name: "test server"
    host: "imap.example.com"
    tls:
      mode: starttls
    login:
      username: "user@example.com"
      password: "secret"
    mailboxes:
      - name: "INBOX"
