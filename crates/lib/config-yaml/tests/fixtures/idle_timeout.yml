servers:
  - name: "test server"
    host: "imap.example.com"
    idle_timeout_secs: 120
    tls:
      mode: implicit
    login:
      username: "user@example.com"
      password: "secret"
    mailboxes:
      - name: "INBOX"
