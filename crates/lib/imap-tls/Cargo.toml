[package]
name = "imap-tls"
version = "0.1.0"
edition = "2024"

[dependencies]
async-imap = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["io-util", "net"] }
