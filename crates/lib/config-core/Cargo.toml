[package]
name = "config-core"
version = "0.1.0"
edition = "2024"
publish = false

[features]
serde = ["dep:serde"]

[dependencies]
serde = { workspace = true, optional = true, features = ["derive"] }
