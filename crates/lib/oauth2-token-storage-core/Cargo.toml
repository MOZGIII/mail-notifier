[package]
name = "oauth2-token-storage-core"
version = "0.1.0"
edition = "2024"
publish = false

[features]
default = []
serde = ["dep:serde"]

[dependencies]
serde = { workspace = true, features = ["derive"], optional = true }
thiserror = { workspace = true }
